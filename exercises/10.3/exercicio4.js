const maiusculator = string => string.toUpperCase();
const firstLetter = string => string.charAt(0);
const concatStrings = (s1, s2) => s1.concat(s2);

module.exports = { maiusculator, firstLetter, concatStrings };
